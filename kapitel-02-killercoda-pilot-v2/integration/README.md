# Integration

Ordner in den Repository-Stamm kopieren:

```text
kapitel-02-killercoda-pilot-v2/
```

Für den Test in einer expliziten Root-`structure.json` ergänzen:

```json
{ "path": "kapitel-02-killercoda-pilot-v2" }
```

Die vorhandene Strukturdatei wird vom Paket nicht automatisch geändert.
